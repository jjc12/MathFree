#include <vector>
#include <iostream>
#include <algorithm>

class Euclid{
	public: //I still need a constructor for this class.
		static int gcd(int num1, int num2);
		static int lcm(int num1, int num2);
		static bool isPrime(int num1);
		static void listOfPrimes(int num1);
		static void linDio(int num1, int num2, int num3);
};